#include <stdio.h>
int main()
{
    float a, b, c;
    printf("enter the value of a:");
    scanf("%f", &a);
    printf("enter the value of b:");
    scanf("%f", &b);
    printf("enter the value of c:");
    scanf("%f", &c);
    if (
        a > b && b > c)
        ;
    printf("max.no.is a");
    elif (b > a && a > c);
    printf("max no.is b");
    elif (
        printf("max no.is c"));
}